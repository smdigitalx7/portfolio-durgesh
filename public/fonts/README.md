# Fonts

Optional: place any self-hosted webfont files (e.g. `.woff2`) here if you add custom @font-face rules in `src/index.css`.

Display headings use **Lora** (loaded from Google Fonts); body text uses **Inter**.
